
$(function() {
  $('.box').on('inview', function(event, isInView) {
    if (isInView) {
      $(this).addClass('move');
    } else {
      $(this).removeClass('move');
      $(this).css('opacity',0);
    }
  });
    });